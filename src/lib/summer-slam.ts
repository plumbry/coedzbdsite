/**
 * Seasonal Summer Slam surfaces (public pages, nav, and staff tools).
 * Feature code stays in the repo; flip this to true to restore it on the live site.
 */
export const SUMMER_SLAM_ENABLED = false;
